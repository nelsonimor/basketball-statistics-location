server:
  port: 8082

spring:
  application:
    name: Basketball-Statistics-Location
  sql:    init:      mode: always
  jpa:
    defer-datasource-initialization: true
    show-sql: true
    generate-ddl: true
    hibernate:      ddl-auto: create
